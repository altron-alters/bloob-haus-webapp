---
layout: layouts/base.njk
---
<article class="page-article" {% if _bloob_unlisted %}data-pagefind-ignore{% else %}data-pagefind-body{% endif %}>
  <header class="page-header">
    <h1 class="page-title">{{ (title_md if title_md else title) | mdinline | safe }}</h1>
    {% if subtitle %}<p class="page-subtitle" data-pagefind-meta="subtitle[content]" content="{{ subtitle }}">{{ subtitle | mdinline | safe }}</p>{% endif %}
  </header>

  {# Filename indexed for pagefind search — both hyphenated and space-separated forms #}
  {% if slug %}
  <span aria-hidden="true" style="position:absolute;clip:rect(0,0,0,0)">{{ slug }}{% if slug_spaced %} {{ slug_spaced }}{% endif %}</span>
  {% endif %}

  <div class="page-content">{{ content | safe }}</div>
</article>
